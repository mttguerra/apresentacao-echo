{
  "name": "apresentacao-echo",
  "version": "1.0.0",
  "description": "Deck web Echo Agency — slides como imagens",
  "type": "module",
  "scripts": {
    "dev": "vite --port 8080 --host",
    "build": "vite build",
    "preview": "vite preview --port 8080"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
