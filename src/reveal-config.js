export const revealConfig = {
  hash: true,
  history: false,
  controls: false,
  progress: true,
  slideNumber: 'c/t',
  transition: 'slide',
  transitionSpeed: 'default',
  backgroundTransition: 'none',
  keyboard: {
    27: null,  // Esc desabilitado (evita saída acidental no palco)
    79: null,  // O desabilitado
  },
  navigationMode: 'linear',
};
